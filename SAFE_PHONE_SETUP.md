# 掌心窗安全轻量版（个人自用补丁）

本补丁保留原项目名称、来源与许可证，仅用于设备所有者个人自用。

## 默认共享范围

- 时间、电量、充电、网络、屏幕开关
- 当前 App 名称
- 当日屏幕时间与解锁次数
- 归电状态

不会持续上传屏幕文字、无障碍节点、截图、常用 App 排名、城市天气、经期状态或应用门禁配置。无障碍服务只识别当前前台 App，不具备读屏、截图、点击或滑动能力。MCP 不暴露任意点击、滑动、文字输入、自动评论、应用门禁或打开 App 等控制工具。

## 可用工具

- `linjian_status`
- `get_life_state`
- `send_notification`
- `set_alarm`
- `get_guidian_state`
- `trigger_guidian`

通知、闹钟和归电只应在设备所有者明确要求时调用。

## MCP 地址

部署后需要在 Render 环境变量中查看 `MCP_ACCESS_KEY`，连接地址为：

```text
https://你的-mcp-服务.onrender.com/mcp/你的MCP_ACCESS_KEY
```

这个完整地址等同于一把私密钥匙，不要截图、转发或公开。

## Android 设置

手机端后端地址必须使用 Render 提供的 `https://...onrender.com` 地址。安全版关闭明文 HTTP，不接受局域网 `http://` 服务。

建议轮询间隔为 `15000` 毫秒。无障碍权限仅用于归电与系统动作；若只查看电量等基础状态，可以先不开使用情况权限，之后再按需要开启。
